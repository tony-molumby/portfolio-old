import React, {Component} from 'react';


class Bio extends component {
    
    render() {
        return (
            <div id={this.props.id}>
            
            </div>
        )
    }
} 

export default Bio;